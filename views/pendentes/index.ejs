<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eletrônica Domingos - Ordens Pendentes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            min-height: 100vh;
            background-color: lightskyblue;
        }

        .principal {
            display: flex;
            flex-direction: column;
            background-color: white;
            min-height: 90vh;
            max-width: 1200px;
            margin: 20px auto;
            box-shadow: 0 0 25px rgba(0,0,0,0.25);
            border-radius: 15px;
            overflow: hidden;
        }

        .cabecalho {
            background-color: #004aad;
            padding: 20px;
            text-align: center;
            color: white;
        }

        .logo {
            font-size: 36px;
            font-weight: bold;
            margin: 0;
            letter-spacing: 1px;
        }

        .container-fluid {
            display: flex;
            flex: 1;
            padding: 0;
            gap: 30px;
        }

        .parte-botoes {
            flex: 1;
            background: #f8f9fa;
            padding: 30px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .parte-pendentes {
            flex: 2;
            padding: 30px 20px;
            background: white;
            overflow-y: auto;
        }

        .botao-dashboard {
            width: 100%;
            max-width: 250px;
            padding: 20px 15px;
            margin: 10px 0;
            font-size: 16px;
            border-radius: 12px;
            background-color: #004aad;
            color: white;
            border: none;
            transition: all 0.3s ease;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .botao-dashboard i {
            margin-right: 10px;
            font-size: 18px;
        }

        .botao-dashboard:hover {
            background-color: #0069d9;
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0,0,0,0.15);
        }

        .botao-dashboard:active {
            transform: translateY(1px);
        }

        h2 {
            color: #004aad;
            margin-bottom: 25px;
            padding-bottom: 10px;
            border-bottom: 2px solid #004aad;
            text-align: center;
            font-weight: bold;
        }

        a {
            text-decoration: none;
            color: white;
        }

        .voltar-dashboard {
            color: white;
            font-size: 18px;
            text-decoration: none;
            display: flex;
            align-items: left;
            justify-content: left;
        }

        .voltar-dashboard i {
            margin-right: 8px;
        }

        @media (max-width: 768px) {
            .container-fluid {
                flex-direction: column;
            }
        }

        .card-custom {
            border: 2px solid #004aad;
            border-radius: 15px;
            padding: 15px;
            margin-bottom: 20px;
            background: #fff;
            text-align: center;
            box-shadow: 0 3px 6px rgba(0,0,0,0.1);
        }

        .card-custom h5 {
            color: #004aad;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .card-custom p {
            margin: 5px 0;
        }

        .card-custom strong {
            color: #004aad;
        }

        .card-custom img {
            max-width: 180px;
            max-height: 180px;
            border-radius: 10px;
            margin: 10px 0;
        }

        .card-custom a {
            display: block;
            margin-top: 10px;
            color: #004aad;
            font-weight: bold;
            text-decoration: none;
        }

        .card-custom a:hover {
            text-decoration: underline;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    
    <div class="principal">
        <div class="cabecalho">
            <h1 class="logo">Eletrônica Domingos</h1>
            <a href="/inicial" class="voltar-dashboard">
                <i class="fas fa-arrow-left"></i> Voltar
            </a>
        </div>

        <div class="container-fluid">
            <div class="parte-botoes">
                <button class="botao-dashboard">
                    <i class="fas fa-plus-circle"></i><a href="/pendentes/novo-cliente">Novo Conserto</a>
                </button>
                <button class="botao-dashboard">
                    <i class="fas fa-clock"></i><a href="/pendentes">Consertos Pendentes</a>
                </button>
                <button class="botao-dashboard">
                    <i class="fas fa-check-circle"></i><a href="/concluidos">Consertos Concluídos</a>
                </button>
                <button class="botao-dashboard">
                    <i class="fas fa-ban"></i><a href="/cancelados">Ordens Canceladas</a>
                </button>
            </div>
        </div>
            <div class="row">
            <% if (ordens.length > 0) { %>
                <% ordens.forEach(o => { %>
                    <div class="col-md-4">
                        <div class="card-custom">
                            <h5><%= o.data_registro.toLocaleDateString("pt-BR") %></h5>

                            <p><strong>Cliente:</strong> <%= o.cliente_nome %></p>
                            <p><strong>Aparelho:</strong> <%= o.aparelho_nome %></p>
                            <p><strong>Problema:</strong> <%= o.aparelho_problema %></p>

                            <% if (o.fotos) { %>
                                <img src="<%= o.fotos %>" alt="Foto do aparelho">
                            <% } %>

                            <a href="/detalhes/<%= o.id %>">Clique para ver detalhes</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12 text-center">
                    <p>Nenhuma ordem pendente encontrada</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
