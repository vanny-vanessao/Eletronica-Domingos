<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Detalhes da Ordem</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .card-detalhes {
            border: 2px solid #0d6efd;
            border-radius: 15px;
            padding: 20px;
            margin-top: 30px;
            background: #fff;
            box-shadow: 0 3px 6px rgba(0,0,0,0.1);
        }

        .card-detalhes h3 {
            color: #0d6efd;
            font-weight: bold;
            margin-bottom: 20px;
            text-align: center;
        }

        .card-detalhes p {
            margin: 8px 0;
            font-size: 16px;
        }

        .card-detalhes strong {
            color: #0d6efd;
        }

        .card-detalhes img {
            display: block;
            margin: 20px auto;
            max-width: 250px;
            max-height: 250px;
            border-radius: 12px;
        }

        .btn-actions {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
<div class="container">
    <a href="/ordens/pendentes" class="btn btn-secondary mt-3">Voltar</a>

    <div class="card-detalhes">
        <h3>Detalhes da Ordem</h3>

        <p><strong>Data de Registro:</strong> <%= ordem.data_registro.toLocaleDateString("pt-BR") %></p>
        <p><strong>Data Estimada:</strong> <%= ordem.data_estimada ? ordem.data_estimada.toLocaleDateString("pt-BR") : "Não informada" %></p>
        <p><strong>Data de Entrega:</strong> <%= ordem.data_entrega ? ordem.data_entrega.toLocaleDateString("pt-BR") : "Não entregue ainda" %></p>

        <p><strong>Cliente:</strong> <%= ordem.cliente.nome %></p>
        <p><strong>Aparelho:</strong> <%= ordem.aparelho.nome %></p>
        <p><strong>Problema:</strong> <%= ordem.aparelho.problema %></p>

        <p><strong>Orçamento:</strong> R$ <%= ordem.orcamento %></p>
        <p><strong>Valor:</strong> R$ <%= ordem.valor %></p>

        <p><strong>Observações:</strong> <%= ordem.registro_obs || "Nenhuma observação" %></p>

        <% if (ordem.aparelho_fotos) { %>
            <img src="<%= ordem.aparelho_fotos %>" alt="Foto do aparelho">
        <% } %>

        <div class="btn-actions">
            <form action="/ordens/<%= ordem.id %>/concluir" method="POST">
                <button type="submit" class="btn btn-success">Concluir</button>
            </form>
            <form action="/ordens/<%= ordem.id %>/cancelar" method="POST">
                <button type="submit" class="btn btn-danger">Cancelar</button>
            </form>
        </div>
    </div>
</div>
</body>
</html>
